feat(frontend): 移动端图表全屏+横屏+缩放体验优化

修复 360px 屏幕模拟交易/回测/指标详情图表 legend 遮挡曲线、
无法横屏放大查看数据的问题。新增可复用组件 + 在三个高频
页面接入。

新增可复用基础设施：
- composables/useChartFullscreen.ts
  · 调 Fullscreen API + screen.orientation.lock('landscape')
  · iOS 等不支持时回退到 CSS 90° 旋转兜底类 .chart-fs-fallback
  · 进/出全屏自动多次 chart.resize() 适配新尺寸
  · 监听 fullscreenchange + ESC 主动退出，组件卸载自动清理
- components/ChartFullscreenBtn.vue
  · 仅 isMobile 显示，32x32px 右上角浮动按钮
  · 全屏态切换图标 FullScreen <-> Close
- styles/index.scss 全局 .chart-wrap:fullscreen 与 .chart-fs-fallback

接入页面：
- paper-trading 历史收益图：
  · legend 改 type:'scroll' 防 4 项标签换行遮挡曲线
  · 响应式 grid mobile {left:42,right:16,top:56,bottom:30}
  · 内层 320→380px on mobile
  · dataZoom 启用 zoomOnMouseWheel / moveOnMouseMove
- backtest-detail 收益走势主图（同模式）
- indicator/index K线图（同模式）

验证：vue-tsc + vite build ✓ vitest 110/110 ✓ dist 已部署
