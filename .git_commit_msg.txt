fix(frontend): 模拟交易详情页移动端布局修复

问题（来自用户实机截图）：
- 顶部标题条策略名被"其他操作"按钮挤压，竖排显示
- 指标条单元过宽，¥99,7... 被截断在屏外
- 左侧 tab 占 64px，Chinese label 竖排，正文剩余横向空间不足
- section 左右内边距 16/20px 加剧内容拥挤

修复（@media max-width:767.98px）：
1. .jq-detail-header 改纵向 flex，策略名 flex-basis:100% 独占一行
2. .jq-metrics-bar 单元 padding 18→12px、数值 18→15px、加 scroll-snap-x
3. .jq-side-tabs 宽 64→52px、item 高 60→54px、字号缩小
4. .jq-section padding 16/20→12/10px；section-header 允许换行
5. .jq-chart-toolbar 左对齐换行；按钮 padding/font 缩小
6. .jq-stats-grid 5 列→2 列
7. 持仓/下单 table 字号缩小到 12px

验证：vue-tsc + vite build ✓；dist 已部署到 quantia/web/static
