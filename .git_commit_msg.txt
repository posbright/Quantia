fix(frontend): 二轮页面横扫 3 处移动适配缺陷

按"再次审查其他页面"请求，扫描全部 views/* 后修复以下 3 个真实问题：

1. customIndicator/index.vue —— 整页缺移动 @media
   - 顶层 7/17 el-col 不折叠：mobile 强制 100% 宽 + 上下堆叠
   - 列表面板限高 60vh，避免占满屏挤掉编辑区
   - 表单 el-row 内 12/6/8 列 → 全部单列
   - el-form 标签 120 → 88px
   - bt-metrics 4 卡 flex → 2x2 grid
   - 表格字号 11px、cell padding 6px
   - 顶部按钮组允许换行、过滤栏 radio 横向滚动

2. settings/token-usage.vue —— el-col 缺响应式断点
   - 4 个 :span=6 汇总卡 + 2 个 :span=12 图表卡在 xs<768 仍占 6/12
   - 加 :xs="12"/:xs="24"，靠 Element Plus 内置 xs 断点折叠

3. verify/fusion.vue —— 移动 @media 未覆盖网格
   - 768 块新增 overlap-grid → 1 列、dim-items → 1 列、kpi-row gap 收紧
   - 480 块新增 kpi-row → 1 列（避免 4 卡挤在 375px 屏）

误报排查（subagent 标记但已有适配，未改动）：
- verify/optimize.vue @768 已有 summary-strip→2 列规则
- home/index.vue 已有 991/767/575 三层断点
- algo/backtest-detail.vue stock-summary @768 已→2 列
- algo/edit.vue metrics-row @992 已→2 列

验证：vite build ✓ vitest 110/110 ✓ dist 已部署
