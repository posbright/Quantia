fix(frontend): 二轮审查 3 处移动适配缺陷修复

1. backtest-detail K线 grid 响应式（chart 缺陷 4）
   - echarts grid[]/dataZoom 之前硬编码 left:58 right:62 px
   - mobile 时改为 left:40 right:16，回收约 14% 横向空间

2. auth store bootstrap 增加 3s 超时（§1.8）
   - 弱网下 /api/auth/me 慢响应会无限阻塞路由守卫
   - 用 Promise.race 兜底，超时走 catch 回退到 enabled=false

3. 字体栈补 Android 国产字体回退（§1.10 问题 1）
   - styles/index.scss 增加 HarmonyOS Sans SC / MiSans / OPPO Sans / vivo Sans / Source Han Sans CN
   - 解决华为/小米/OPPO/vivo 设备 fallback 到系统默认 Roboto 字号过小问题

验证：
- build✓
- vitest 110/110 ✓
- smoke 10/10 ✓
- dist 已部署

其他审计结果（无需改）：
- 所有 100vh→100dvh 迁移已完成
- 所有 el-dialog width 已为 min(Npx, 92vw) 模式
- chart 缺陷 1/5/6/7 已在前期 commit 完成
- useVirtualKeyboard 已在 login/register 接入
