fix(frontend): 首页"今日精选/行业资金流向"空数据修复

根因：/api_data 后端返回 {columns, data, total}，
而 home/index.vue loadPicks/loadFundFlow 读取 r.rows，
始终为 undefined → 空数组 → 走空态占位文案。

修复：
- loadPicks / loadFundFlow 取数改为 r.data || r.rows
  （保留 rows 兜底，与 loadIndexes/loadSelection 一致）
- pickField 候选字段补齐真实表列：
  · cn_stock_strategy_enter 涨跌幅 = p_change（前置最优先）
  · cn_stock_fund_flow_industry 净流入 = fund_amount（前置最优先）

验证：localhost 黑盒
  /api_data?name=cn_stock_strategy_enter → total=5388, data[].p_change ✓
  /api_data?name=cn_stock_fund_flow_industry → total=13888, data[].fund_amount ✓
vite build ✓ dist 已部署
